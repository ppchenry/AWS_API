{
  "name": "aws-api-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --runInBand"
  },
  "jest": {
    "testTimeout": 60000,
    "setupFilesAfterEnv": [
      "<rootDir>/jest.setup.cjs"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mongoose": "^9.4.1"
  }
}
