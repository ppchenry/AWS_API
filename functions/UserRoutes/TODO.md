# TODO for UserRoutes

- [ ] Create middleware for guarding the Lambda (authentication/authorization checks)
- [ ] Figure out the correct paths in routing based on the AWS API Gateway `resource` (see template.yaml)
- [ ] Check all service handlers and modify as needed for clean code, performance, and security
- [ ] Write events for UserRoutes to enable SAM CLI API testing
- [ ] Use Zod to validate request body fields for all endpoints
