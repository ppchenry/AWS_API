const express = require("express");
const router = express.Router();
// const {conn} = require("../config/db");

// const UserSchema = require("../models/User");
// const User = conn.model('User', UserSchema, 'users');
// const ImageCollectionSchema = require("../models/ImageCollection");
// const ImageCollection = conn.model('ImageCollection', ImageCollectionSchema, 'image_collection');

// const ContentBackupSchema = require("../models/ContentBackup");
// const ContentBackup = conn.model("ContentBackup", ContentBackupSchema, "content_backup")

// const ContentSchema = require("../models/Content");
// const Content = conn.model("Content", ContentSchema, "content")
// const multer = require("multer");
// const adminAuth = require("../../middlewares/adminAuth");
// const validation = require("../../utils/validation");
// const { promptTest } = require("../../utils/openAI");
// const aws = require("../../utils/awsBucket");
// const fs = require("fs");
// const https = require("https");

// const storage = multer.memoryStorage();
// const upload = multer({
//   limits: { fileSize: 15 * 1024 * 1024 },
// });
// const upload2 = multer({
//   dest: "data/",
// });

// Pages
adoptionPage = "main/adoption"
adoptionDetailPage = "main/adoption-details";

//Get - ADOPTION
router.get("/", (req, res) => {
  res.render(adoptionPage);
});

router.get("/:id", (req, res) => {
  res.render(adoptionDetailPage);
});









module.exports = router;
